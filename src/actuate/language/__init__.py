"""L6 — Language & Task Annotation.

Spec: docs/architecture/MASTER_IMPLEMENTATION_SPEC.md §L6

Multi-paraphrase, structured hand+object+action captions, subtask spans,
subgoal-frame anchors at phase boundaries. Gate: a hallucinated-caption fixture
must be FLAGGED, not passed.

NOT IMPLEMENTED. Increment 1 builds the foundation only (schema, io, catalog, infra).
This package is a placeholder so the import-linter contract has the full layer graph to
check against, and so nothing is quietly built out of order.
"""
